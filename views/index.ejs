<!DOCTYPE html>
<html>

<head>
  <title>
    <%= title %>
  </title>
  <link rel='stylesheet' href='/stylesheets/style.css' />
</head>

<body>
  <h1>
    <%= title %>
  </h1>
  <p>Welcome to <%= title %>
  </p>
  <% if (loggedIn) { %>
    <img src="<%= user.profilePicture %>">
    <p>
      You are logged in as <%= user.displayName %> and your short lived token is <%= user.accessToken %>
    </p>
    <a href="/auth/logout">Log out</a>
    <% } else {%>
      <a href="/auth/strava">Login with Strava</a>
      <% }%>
</body>

</html>